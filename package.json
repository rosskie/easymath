{
  "name": "easymath",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "lint": "biome check --write ./",
    "preview": "yarn build && vite preview --open"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "typescript": "5.6.2",
    "vite": "^5.4.1"
  }
}
